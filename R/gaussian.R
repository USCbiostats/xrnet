# Function to prepare Gaussian outcome for model fitting

gaussian <- function(x,
                     y,
                     external,
                     penalty,
                     weights,
                     isd,
                     intr,
                     control) {

    y <- as.double(drop(y))
    ka <- as.integer(1)

    #fit <- gaussian_fit(ka, )
}
